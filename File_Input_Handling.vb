Module File_Input_Handling
    Sub Parse_input_File(ByRef In_Filename As String)
        'Routine to import information as requested into an internal datastructure, ready for writing out later.

    End Sub
End Module
